---
"@react-ai-form/react-hook-form": patch
---

Rewrite README to document the v3 adapter API (`useAIForm`, `AIFormField`, `AITextField`, `AIFormStatusProvider`). The previous README still described the package as "under active development" and showed the pre-v3 manual `setValue` / `trigger` pattern.
